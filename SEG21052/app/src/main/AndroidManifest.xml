<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.ahame_000.seg2105">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <activity android:name=".CreateChoreActivity" />
    <activity android:name=".ProfileListActivity">

    </activity>
    <activity android:name=".CreateAccountActivity"/>
    <activity android:name=".EditViewChoreActivity" />
    <activity android:name=".AddMemberActivity" />
    <activity android:name=".ChoreListActivity">

    </activity>
    <activity android:name=".AccountLoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

    </activity>
</application>


</manifest>