<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.ahame_000.seg2105">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <activity android:name=".Activities.CreateChoreActivity" >

    </activity>
    <activity android:name=".Activities.ProfileLoginActivity"></activity>
    <activity android:name=".Activities.ProfileListActivity">

    </activity>

    <activity android:name=".Activities.CreateAccountActivity"/>
    <activity android:name=".Activities.EditViewChoreActivity" >

    </activity>
    <activity android:name=".Activities.AddMemberActivity" />
    <activity android:name=".Activities.NavigationActivity">

    </activity>
    <activity android:name=".Activities.AccountLoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>


    </activity>
</application>


</manifest>